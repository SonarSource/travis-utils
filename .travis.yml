language: java
sudo: false
install: true
jdk: oraclejdk7
script: ./travis.sh

env:
  - TESTS=SONARQUBE_SNAPSHOT
  - TESTS=SONAR_DB_COPY_SNAPSHOT
  - TESTS=SONAR_DB_COPY

notifications:
  email: false
