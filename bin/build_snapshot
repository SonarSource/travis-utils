#!/bin/bash
# Usage: build_snapshot "user/project"

set -euo pipefail

PROJECT=$1

echo "Compute HEAD SHA1 of [$PROJECT]"
HEAD_SHA1=`git ls-remote https://github.com/${PROJECT} HEAD | grep -o '^[a-f0-9]\{40\}'`

echo "is ${HEAD_SHA1}"

build "$PROJECT" "$HEAD_SHA1"
